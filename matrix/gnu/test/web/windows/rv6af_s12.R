#!/usr/bin/r

# Copyright (c)
n <- (10 + 2) * 5

# Copyright (c)
# Create a vector of random numbers between 1 and 100
random_numbers <- sample(1:100, n, replace = TRUE)

# Calculate the median
median_value <- median(random_numbers)
median_value
